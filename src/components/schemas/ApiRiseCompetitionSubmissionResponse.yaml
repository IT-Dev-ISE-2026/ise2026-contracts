title: ApiRiseCompetitionSubmissionResponse
type: object
required: [id, stage, fileType, fileUrl, createdAt]
properties:
  id:
    type: string
    format: uuid
    readOnly: true
  stage:
    type: string
    enum: [penyisihan, semifinal, final]
    readOnly: true
  fileType:
    type: string
    enum: [proposal, presentation, rta]
    readOnly: true
  fileUrl:
    type: string
    format: uri
    readOnly: true
  createdAt:
    type: string
    format: date-time
    readOnly: true
