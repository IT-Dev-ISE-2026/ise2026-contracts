type: object
required: [id, slug, name, price, is_active, timelines, links]
properties:
  id:
    type: integer
    example: 1
  parent_id:
    type: integer
    nullable: true
    example: null
  slug:
    type: string
    example: bionix
  name:
    type: string
    example: Bionix Competition
  price:
    type: integer
    example: 100000
  is_active:
    type: boolean
    example: true
  timelines:
    type: array
    items:
      $ref: './ApiEventTimeline.yaml'
  links:
    type: array
    items:
      $ref: './ApiEventLink.yaml'
  sub_events:
    type: array
    items:
      $ref: './ApiEvent.yaml'
