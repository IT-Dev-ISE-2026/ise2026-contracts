type: object
required: [totalItems, totalPages, currentPage, limit]
properties:
  totalItems:
    type: integer
    format: int64
    example: 100
  totalPages:
    type: integer
    example: 10
  currentPage:
    type: integer
    example: 1
  limit:
    type: integer
    example: 10
