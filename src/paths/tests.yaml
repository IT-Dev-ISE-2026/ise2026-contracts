test-email-verification:
  post:
    operationId: testEmailVerification
    description: Test email verification workflow
    summary: Test email verification
    tags: [Tests]
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [email]
            properties:
              email:
                type: string
                format: email
                example: dev@example.com
          example:
            email: dev@example.com
    responses:
      '200':
        description: Verification simulation triggered
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
            examples:
              SuccessTrigger:
                summary: Berhasil mentrigger simulasi
                value:
                  message: Simulation email verification for dev@example.com has been sent.

test-orders:
  post:
    operationId: testOrder
    description: Create a dummy order for testing. Requires authentication and user permission.
    summary: Test order
    tags: [Tests]
    security:
      - BearerAuth: []
    responses:
      '201':
        description: Order created
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
                data:
                  type: object
            examples:
              OrderCreated:
                summary: Berhasil membuat order dummy
                value:
                  message: "Order created successfully"
                  data:
                    order_id: "ORD-TEST-123"
                    status: "pending"
      '403':
        description: Forbidden - Requires user permission
        content:
          application/json:
            schema:
              $ref: '../components/schemas/ApiError.yaml'
            examples:
              MissingPermission:
                summary: User tidak memiliki permission 'user'
                value:
                  message: "forbidden: you do not have permission 'user'"
                  requestId: req-test-403

test-admin:
  get:
    operationId: testAdminAccess
    description: Test admin access level. Requires authentication and admin permission.
    summary: Test admin access
    tags: [Tests]
    security:
      - BearerAuth: []
    responses:
      '200':
        description: Access granted
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
            examples:
              AdminAccess:
                summary: Akses admin diverifikasi
                value:
                  message: "Welcome Admin! Access granted."
      '403':
        description: Forbidden
        content:
          application/json:
            schema:
              $ref: '../components/schemas/ApiError.yaml'
            examples:
              NotAnAdmin:
                summary: User bukan admin
                value:
                  message: "access denied: admin role required"
                  requestId: req-admin-403

test-admin-bionix:
  get:
    operationId: testAdminBionixRead
    description: Test Bionix admin read access.
    summary: Test Bionix admin read
    tags: [Tests]
    security:
      - BearerAuth: []
    responses:
      '200':
        description: Access granted
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
            examples:
              BionixReadAccess:
                summary: Akses baca bionix diverifikasi
                value:
                  message: "Access granted for Bionix Read"
  post:
    operationId: testAdminBionixWrite
    description: Test Bionix admin write access.
    summary: Test Bionix admin write
    tags: [Tests]
    security:
      - BearerAuth: []
    responses:
      '200':
        description: Access granted
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
            examples:
              BionixWriteAccess:
                summary: Akses tulis bionix diverifikasi
                value:
                  message: "Access granted for Bionix Write"

