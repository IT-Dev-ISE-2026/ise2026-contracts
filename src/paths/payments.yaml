webhook:
  post:
    tags:
      - Payments
    summary: Midtrans Payment Webhook
    description: Receives notification callback from Midtrans regarding payment status updates.
    operationId: processPaymentWebhook
    requestBody:
      required: true
      content:
        application/json:
          schema:
            type: object
            description: Arbitrary Midtrans JSON payload containing order_id, transaction_status, etc.
    responses:
      '200':
        description: Webhook processed successfully.
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string

sync:
  post:
    tags:
      - Payments
    summary: Manual Payment Sync
    description: >-
      Manually synchronizes pending payment attempts with Midtrans and
      reconciles the order against its business payment deadline. A failed
      Midtrans attempt does not invalidate an order while the order payment
      deadline is still active.
    operationId: syncPaymentStatus
    security:
      - BearerAuth: []
    parameters:
      - name: orderId
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: The ID of the order to sync.
    responses:
      '200':
        description: Payment synced successfully.
        content:
          application/json:
            schema:
              type: object
              properties:
                message:
                  type: string
                status:
                  type: string
                  description: The new payment status.
      '401':
        description: Unauthorized.
        content:
          application/json:
            schema:
              $ref: '../components/schemas/ApiError.yaml'
      '403':
        description: Forbidden.
        content:
          application/json:
            schema:
              $ref: '../components/schemas/ApiError.yaml'
      '404':
        description: Order not found.
        content:
          application/json:
            schema:
              $ref: '../components/schemas/ApiError.yaml'

active:
  post:
    tags:
      - Payments
    summary: Ensure Active Payment
    description: >-
      Synchronizes pending payment attempts and returns an active Midtrans Snap
      transaction for an unpaid order. If the previous attempt has expired,
      been cancelled, or been denied, a replacement transaction is created
      without extending the order payment deadline. The operation is rejected
      once the business payment deadline has passed.
    operationId: ensureActivePayment
    security:
      - BearerAuth: []
    parameters:
      - name: orderId
        in: path
        required: true
        schema:
          type: string
          format: uuid
        description: The ID of the order that needs an active payment transaction.
    responses:
      '200':
        description: Active payment transaction is available.
        content:
          application/json:
            schema:
              type: object
              properties:
                data:
                  type: object
                  properties:
                    orderId:
                      type: string
                      format: uuid
                    finalPrice:
                      type: integer
                    midtransOrderId:
                      type: string
                    snapToken:
                      type: string
                    redirectUrl:
                      type: string
                      nullable: true
                    amount:
                      type: integer
                    sequence:
                      type: integer
                    paymentDeadline:
                      type: string
                      format: date-time
                      nullable: true
                    status:
                      type: string
                  required:
                    - orderId
                    - finalPrice
                    - midtransOrderId
                    - snapToken
                    - amount
                    - sequence
                    - status
              required:
                - data
      '401':
        description: Unauthorized.
        content:
          application/json:
            schema:
              $ref: '../components/schemas/ApiError.yaml'
      '403':
        description: Forbidden.
        content:
          application/json:
            schema:
              $ref: '../components/schemas/ApiError.yaml'
      '404':
        description: Order not found.
        content:
          application/json:
            schema:
              $ref: '../components/schemas/ApiError.yaml'
      '409':
        description: Order is already paid.
        content:
          application/json:
            schema:
              $ref: '../components/schemas/ApiError.yaml'
      '410':
        description: The order payment deadline has passed.
        content:
          application/json:
            schema:
              $ref: '../components/schemas/ApiError.yaml'
